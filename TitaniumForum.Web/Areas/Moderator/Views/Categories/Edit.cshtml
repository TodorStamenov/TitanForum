@model CategoryFormServiceModel

@{
    ViewBag.Title = "Edit Category";
}

<div class="row">
    <div class="col-md-offset-2 col-md-6">
        <h2>@ViewBag.Title</h2>
        @using (Html.BeginForm("Edit", "Categories", FormMethod.Post))
        {
            Html.RenderPartial("_CategoryForm", Model);
        }
    </div>
</div>