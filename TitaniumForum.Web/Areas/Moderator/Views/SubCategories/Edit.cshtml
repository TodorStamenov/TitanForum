@model SubCategoryFormViewModel

@{
    ViewBag.Title = "Edit Sub Category";
}

<div class="row">
    <div class="col-md-offset-2 col-md-8">
        <h2 class="col-md-offset-2">@ViewBag.Title</h2>
        @using (Html.BeginForm("Edit", "SubCategories", FormMethod.Post, new { @class = "form-horizontal" }))
        {
            Html.RenderPartial("_SubCategoryForm", Model);
        }
    </div>
</div>